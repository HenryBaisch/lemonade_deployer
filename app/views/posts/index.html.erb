<!-- upcoming shows -->
<div class="container-fluid card-dark px-0">
  <h2 class="spotify-label mt-4 mb-0 mx-2 mt-2">Upcoming Shows</h2>

  <div id="posts" class="horizontal-scroll-container container-fluid mt-2" >
    <% @posts.each do |post| %>
      <div class="container post-card" style="max-width: 200px;">
        <%= render post %>
      </div>
    <% end %>
  </div>
</div>
<!-- local artists -->
<h2 class="spotify-label mt-4 mb-0 mx-2 mt-2">Local Artists</h2>
<div class="container px-0">
  <div class="card card-dark mx-1">
  <!-- add artists iterator -->
  </div>
</div>